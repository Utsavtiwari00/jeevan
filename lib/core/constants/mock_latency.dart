class MockLatency {
  MockLatency._();

  static const Duration short = Duration(milliseconds: 400);
  static const Duration medium = Duration(milliseconds: 700);
  static const Duration standard = Duration(milliseconds: 600);
  static const Duration long = Duration(milliseconds: 1000);
}
